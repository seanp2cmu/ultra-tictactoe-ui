import { AIGame } from '@/src/components/ai-game';

export default function AIGamePage() {
  return (
    <main className="min-h-screen bg-background flex flex-col">
      <AIGame />
    </main>
  );
}
